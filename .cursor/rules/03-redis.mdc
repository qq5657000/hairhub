---
description: Redis 缓存、锁、队列规范
globs:
  - "app/**/*.php"
  - "config/cache.php"
  - "config/database.php"
  - "config/queue.php"
alwaysApply: false
---

# Redis 使用规范

## 1. 基础原则
- Redis 用于缓存、锁、队列、限流。
- Redis 不是核心数据最终来源。
- 关键数据必须落库。
- 所有 Key 必须有命名规范。
- 所有缓存必须设置 TTL。
- 禁止永久缓存业务数据。
- 禁止保存超大对象。
- 禁止缓存敏感明文。
- 禁止使用 KEYS 命令。
- 生产环境禁止 FLUSHALL。
- 生产环境禁止 FLUSHDB。
- 优先使用 Laravel Cache、Redis、Queue 接口。
- 不直接散落原生 Redis 命令。

## 2. Key 命名
- 格式：hairhub:{env}:{module}:{type}:{id}。
- 全部使用小写。
- 分隔符统一使用冒号。
- Key 中不使用空格。
- Key 中不使用中文。
- 环境必须隔离。
- 用户缓存包含 user_id。
- 资源缓存包含资源 ID。
- 列表缓存包含条件摘要。
- 锁 Key 使用 lock 前缀。
- 限流 Key 使用 rate 前缀。
- 幂等 Key 使用 idempotency 前缀。
- 示例：hairhub:prod:user:profile:1001。
- 示例：hairhub:prod:lock:hair_generate:1001。
- 示例：hairhub:prod:rate:sms:138xxxx。
- 不把完整请求参数直接拼入 Key。
- 复杂条件使用稳定 hash。

## 3. TTL
- TTL 按数据变化频率设置。
- 高频变化数据使用短 TTL。
- 稳定配置可使用较长 TTL。
- 用户会话按安全要求设置 TTL。
- 验证码 TTL 通常 5 至 10 分钟。
- 幂等 Key TTL 覆盖重复请求窗口。
- 锁 TTL 必须覆盖最大执行时间。
- TTL 增加少量随机抖动。
- 避免大量 Key 同时过期。
- 不使用 rememberForever。
- TTL 常量集中定义。
- 更新业务数据后主动失效缓存。

## 4. 缓存策略
- 优先 Cache Aside。
- 读：先查缓存，再查数据库。
- 回源后写缓存。
- 写：先写数据库，再删缓存。
- 删除缓存失败需记录日志。
- 高一致性数据不依赖缓存判断。
- 缓存未命中不能改变业务结果。
- 缓存服务异常时降级到数据库。
- 禁止把缓存当唯一状态。
- 列表缓存必须包含筛选条件。
- 权限缓存变更后立即失效。
- 配置缓存变更后立即失效。
- 用户资料缓存按用户维度删除。
- 关联变更需删除相关聚合缓存。

## 5. 穿透、击穿、雪崩
- 不存在数据可短暂缓存空值。
- 空值 TTL 必须短。
- 用户输入先验证，减少无效查询。
- 热点 Key 回源使用互斥锁。
- 锁等待设置超时。
- 获取锁失败可返回旧缓存。
- 大量 Key 使用随机 TTL。
- 批量预热分批执行。
- 禁止同一时刻全量预热。
- Redis 故障时保护数据库。
- 必要时启用接口限流。
- 热点数据可采用逻辑过期。

## 6. 分布式锁
- 使用 Cache::lock。
- 锁必须有 TTL。
- 锁必须有唯一名称。
- 锁粒度尽量小。
- 锁范围只覆盖关键区段。
- 获取锁设置等待时间。
- 使用 block 时捕获超时。
- 使用 finally 释放锁。
- 只允许锁持有者释放。
- 长任务考虑自动续期方案。
- 锁不能替代数据库唯一约束。
- 锁不能替代事务。
- 订单、库存等仍需数据库保护。
- 锁 Key 必须包含业务对象 ID。
- 禁止单个全局锁阻塞全部用户。

## 7. 幂等
- 重复请求使用幂等 Key。
- 幂等 Key 来自客户端或业务单号。
- 幂等 Key 必须绑定用户和接口。
- 首次请求使用 SET NX。
- 处理成功保存结果摘要。
- 处理中状态设置合理 TTL。
- 失败是否允许重试需明确。
- Redis 幂等必须配合数据库唯一约束。
- 第三方回调使用回调单号唯一索引。
- 不仅依赖 Redis 防重复。

## 8. 限流
- 使用 Laravel RateLimiter。
- 限流维度按用户、IP、设备选择。
- 登录接口按账号与 IP 双维度。
- 短信接口按手机号与 IP。
- AI 生成按用户套餐与频率。
- 管理后台限流独立配置。
- 错误次数与正常请求分开。
- 返回统一 429 响应。
- 返回可重试时间。
- 白名单必须配置化。
- 限流 Key 设置 TTL。
- 不使用永久计数器。

## 9. 队列
- 队列按业务拆分。
- 建议：default、ai、image、notification。
- Job 必须设置 timeout。
- Job 必须设置 tries。
- Job 必须设置 backoff。
- Job 必须可重复执行。
- Job 参数尽量传 ID。
- 不传超大数组。
- 不传敏感 Token。
- 外部服务调用设置超时。
- 外部服务重试次数有限。
- 不可重试错误直接失败。
- 失败任务记录业务 ID。
- 关键 Job 使用 afterCommit。
- 高并发任务控制并发数。
- 发布后重启 worker。
- 使用 Supervisor 管理进程。
- 定期清理 failed_jobs。

## 10. 数据结构
- 简单值使用 String。
- 计数器使用 INCR。
- 集合去重使用 Set。
- 排名使用 Sorted Set。
- 队列交给 Laravel Queue。
- Hash 仅用于结构稳定的小对象。
- 禁止单个 Hash 无限增长。
- 禁止单个 List 无限增长。
- 大集合必须设置清理策略。
- 使用 Lua 时保持脚本短小。
- Lua 脚本必须有测试。
- 不滥用 Pub/Sub 承担可靠消息。

## 11. 禁止事项
- 禁止无 TTL 的业务缓存。
- 禁止 Key 无前缀。
- 禁止把数据库整表放 Redis。
- 禁止使用 Redis 保存订单最终状态。
- 禁止使用 Redis 保存余额最终值。
- 禁止循环逐条网络调用。
- 批量操作使用 pipeline。
- 禁止大 Key。
- 禁止热 Key 无保护。
- 禁止无限重试。
- 禁止吞掉 Redis 异常。
- 禁止因缓存失败中断非关键流程。
- 禁止在日志打印完整缓存敏感值。

## 12. 检查项
- Key 是否规范。
- TTL 是否合理。
- 是否有随机抖动。
- 是否有失效逻辑。
- 是否防穿透。
- 是否防击穿。
- 是否防雪崩。
- 是否需要锁。
- 锁是否可释放。
- 是否需要数据库唯一约束。
- Job 是否幂等。
- Job 是否有限重试。
- 是否存在大 Key。
- 是否有监控指标。
