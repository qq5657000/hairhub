---
description: Laravel 10 开发规范
globs:
  - "app/**/*.php"
  - "routes/**/*.php"
  - "config/**/*.php"
  - "database/**/*.php"
alwaysApply: false
---

# Laravel 10 开发规范

## 1. 基础要求
- 使用 Laravel 10 标准能力。
- 使用 PHP 8.2。
- 遵循 PSR-12。
- 优先使用框架原生组件。
- 不重复实现框架已有功能。
- 不直接修改 vendor。
- 不在业务代码读取 $_GET、$_POST。
- 使用 Request 获取输入。
- 使用 config() 获取配置。
- 禁止在业务代码直接调用 env()。
- 时间统一使用 Carbon。
- 集合处理优先使用 Collection。
- 大数据处理使用 chunk、cursor 或 lazy。
- 避免一次性加载大量模型。

## 2. Controller
- Controller 只做接口编排。
- 单个方法尽量不超过 30 行。
- 参数验证交给 FormRequest。
- 业务逻辑交给 Service。
- 数据输出交给 Resource。
- 不在 Controller 开启复杂事务。
- 不在 Controller 编写复杂 SQL。
- 不在 Controller 拼接响应结构。
- 不在 Controller 捕获所有异常。
- 只捕获可处理异常。
- 使用依赖注入。
- 避免 Facade 滥用。
- 一个 Controller 对应一类资源。
- 方法优先使用 index、show、store、update、destroy。
- 非 CRUD 动作使用明确动词。
- 禁止返回 Eloquent 原始模型给公网 API。
- 禁止直接返回异常详情。

## 3. FormRequest
- 所有写接口优先使用 FormRequest。
- 规则写在 rules()。
- 权限写在 authorize()。
- 自定义错误写在 messages()。
- 字段别名写在 attributes()。
- 条件规则使用 Rule。
- 枚举字段使用 Rule::enum。
- 唯一校验必须考虑软删除。
- 更新唯一校验必须忽略当前记录。
- 数组必须校验每一项。
- 文件必须校验 mime、size。
- 日期必须校验格式。
- ID 必须校验存在性。
- 禁止仅依赖前端校验。
- prepareForValidation() 只做轻量标准化。
- 不在 FormRequest 执行业务写入。

## 4. Service
- Service 表达业务用例。
- Service 方法名应清晰。
- 事务边界优先放在 Service。
- 一个事务只覆盖必要写操作。
- 事务中避免调用外部接口。
- 事务中避免长耗时操作。
- 外部调用优先在事务前完成。
- 需要一致性时使用 outbox 或事件补偿。
- Service 不依赖 HTTP 响应对象。
- Service 不直接读取全局请求。
- Service 返回模型、DTO 或明确结果对象。
- Service 可调用 Repository。
- Service 可发布事件。
- Service 可投递 Job。
- Service 不负责视图渲染。
- 核心流程必须处理幂等。
- 关键状态变更必须校验当前状态。
- 关键写入必须防止并发覆盖。

## 5. Repository
- 简单 CRUD 不强制 Repository。
- 复杂查询可使用 Repository。
- Repository 只负责数据访问。
- Repository 不处理 HTTP。
- Repository 不返回 Response。
- Repository 不写业务文案。
- 查询方法名表达筛选目的。
- 返回类型保持明确。
- 分页返回 LengthAwarePaginator。
- 单条查询返回 Model 或 null。
- 更新方法返回受影响结果。
- 避免万能 Repository。
- 避免 BaseRepository 过度抽象。
- 不封装 Eloquent 全部方法。
- 查询条件重复时使用 Scope。

## 6. Model
- 明确 $table。
- 明确 $fillable 或 $guarded。
- 定义 casts。
- 日期字段使用 datetime cast。
- JSON 字段使用 array 或 collection cast。
- 金额字段避免 float cast。
- 状态字段优先枚举 cast。
- 定义关系方法。
- 关系方法使用名词。
- 一对多使用复数。
- 一对一使用单数。
- 常用过滤使用本地 Scope。
- 避免在 Accessor 查询数据库。
- 避免在 Boot 中执行复杂业务。
- 避免在 Observer 触发不可控链路。
- 模型事件只做轻量操作。
- 删除关联数据需明确策略。
- 软删除模型查询需考虑 withTrashed。
- 禁止隐藏式全局 Scope 影响关键查询。
- 必须说明全局 Scope 的影响。

## 7. Eloquent 查询
- 查询字段明确 select。
- 关联查询使用 with。
- 防止 N+1。
- 需要计数使用 withCount。
- 存在性判断使用 exists。
- 不使用 count() > 0。
- 单值查询使用 value。
- 主键查询使用 find。
- 复杂条件使用 when。
- 条件分组使用闭包。
- 批量更新使用 update。
- 批量插入使用 insert 或 upsert。
- 避免循环 save。
- 批量任务使用 chunkById。
- 大表游标使用 lazyById。
- 排序字段必须稳定。
- 分页排序必须包含唯一字段。
- 关联筛选使用 whereHas。
- 仅需关联存在时使用 whereRelation。
- 禁止不受控 get()。
- 禁止在循环中发起相同查询。

## 8. 事务
- 使用 DB::transaction。
- 不手动 begin 后忘记 rollback。
- 事务尽量短。
- 事务中只做数据库操作。
- 事务中锁定必要记录。
- 并发更新使用 lockForUpdate。
- 乐观锁可使用 version 字段。
- 捕获异常后必须重新抛出或转换。
- 禁止吞掉事务异常。
- 死锁需配置有限重试。
- 重试逻辑必须幂等。
- 事务提交后再发送通知。
- 可使用 DB::afterCommit。
- Job 需要 afterCommit 时明确配置。
- 多库事务必须说明不可保证原子性。

## 9. API Resource
- 公网 API 使用 Resource。
- 字段名保持稳定。
- 不暴露内部字段。
- 不暴露软删除字段。
- 不暴露密钥和 Token。
- 时间格式统一。
- 空值策略统一。
- 关联使用 whenLoaded。
- 可选字段使用 when。
- 列表使用 ResourceCollection。
- Resource 不查询数据库。
- Resource 不修改数据。
- Resource 不执行外部请求。
- 分页元数据保持统一。

## 10. 路由
- API 路由放 routes/api.php。
- 后台路由遵循 Dcat Plus。
- 路由使用命名。
- 路由分组统一前缀。
- 中间件配置在路由组。
- REST 资源使用 apiResource。
- 自定义动作避免滥用 POST。
- 路由参数使用单数。
- 绑定优先使用隐式绑定。
- 敏感接口必须认证。
- 管理接口必须权限校验。
- 禁止重复路由。
- 禁止路由闭包承载业务逻辑。
- 版本接口使用 /api/v1。

## 11. 中间件
- 中间件只做横切逻辑。
- 认证、限流、日志适合中间件。
- 不在中间件执行复杂业务。
- 不在中间件写大量数据库。
- 中间件失败返回统一错误。
- 请求 ID 在入口生成。
- 请求日志过滤敏感字段。
- 限流 Key 必须区分用户或 IP。
- 管理端与客户端限流分开。
- 白名单规则必须可配置。

## 12. 异常处理
- 使用业务异常表达可预期错误。
- 业务异常包含错误码。
- 异常消息面向调用方。
- 系统异常记录完整日志。
- 生产环境隐藏堆栈。
- 不使用 Exception 代替所有业务错误。
- 不用 false 表达复杂失败。
- 不用字符串判断异常类型。
- Handler 统一转换响应。
- 404、422、429、500 格式统一。
- 第三方异常转换为内部错误码。
- 超时与服务不可用分开处理。
- 日志包含 request_id。
- 重复错误避免刷屏。

## 13. 队列
- 长耗时任务使用队列。
- Job 必须可重试。
- Job 必须幂等。
- Job 参数尽量只传 ID。
- 避免序列化大对象。
- 设置 timeout。
- 设置 tries。
- 设置 backoff。
- 设置 retryUntil 时明确截止时间。
- 不无限重试。
- 外部接口失败区分可重试与不可重试。
- 失败任务记录业务上下文。
- 需要唯一执行时使用 ShouldBeUnique。
- 防并发时使用 WithoutOverlapping。
- 数据库写入需要考虑重复消费。
- 关键 Job 使用 afterCommit。
- 队列名称按业务分类。
- 生产环境使用 Supervisor。
- 发布后重启 queue worker。

## 14. 事件与监听器
- 事件表达已发生事实。
- 事件名使用过去式。
- 监听器只处理一个职责。
- 慢监听器实现 ShouldQueue。
- 事务后事件使用 afterCommit。
- 不用事件隐藏主业务流程。
- 核心结果必须在 Service 内确定。
- 监听器失败不能破坏主事务。
- 重要副作用需可补偿。
- 事件负载只包含必要数据。

## 15. 缓存
- 缓存使用 Cache 接口。
- Key 统一命名。
- 设置合理 TTL。
- 不永久缓存业务数据。
- 数据更新后删除相关缓存。
- 避免缓存穿透。
- 热点 Key 防止击穿。
- 大对象不放缓存。
- 不缓存 Eloquent 大集合。
- 缓存失败不能影响核心流程。
- 缓存锁使用 lock。
- 缓存规则详见 03-redis.mdc。

## 16. 配置
- 配置文件放 config。
- 环境差异使用环境变量。
- 业务代码使用 config。
- 配置项提供默认值。
- 密钥不提交仓库。
- 配置名使用 snake_case。
- 配置结构不宜过深。
- 发布后执行 config:cache。
- 修改配置需说明清缓存。
- 禁止在运行时修改配置文件。

## 17. 文件与存储
- 使用 Storage。
- 不直接拼接 storage 路径。
- 磁盘通过配置选择。
- 文件名使用 UUID。
- 保存原始文件名需单独字段。
- 校验扩展名和 MIME。
- 图片限制尺寸和大小。
- 私有文件使用临时签名 URL。
- 删除业务记录时处理文件生命周期。
- 外部对象存储失败需记录日志。
- 不在数据库保存大文件二进制。
- 上传后处理使用队列。

## 18. Artisan 命令
- 命令名使用业务前缀。
- 命令支持 --dry-run 时优先提供。
- 批量命令支持断点。
- 批量命令输出进度。
- 批量命令记录开始结束。
- 大数据使用 chunkById。
- 命令避免一次加载全部数据。
- 命令失败返回非零状态码。
- 高风险命令需要确认。
- 定时命令必须防重叠。

## 19. Scheduler
- 定时任务定义清晰。
- 使用 withoutOverlapping。
- 多实例使用 onOneServer。
- 指定时区。
- 失败发送告警。
- 长任务使用队列。
- 任务必须幂等。
- 频繁任务避免全表扫描。
- 任务日志包含执行批次。
- 修改计划任务后说明部署操作。

## 20. Dcat Plus
- 遵循现有后台目录。
- 表格查询避免 N+1。
- Grid 字段只加载必要列。
- Form 校验不可省略。
- 操作按钮必须校验权限。
- 批量操作必须二次确认。
- 高风险操作记录审计日志。
- 不在 Grid 闭包执行大量查询。
- 复杂筛选封装查询。
- 后台状态显示与数据库状态一致。
- 后台导出使用队列处理大数据。
- 后台上传遵循统一存储规范。
- 不覆盖 Dcat Plus 核心文件。
- 自定义扩展放项目目录。

## 21. 测试
- Feature Test 覆盖接口。
- Unit Test 覆盖纯逻辑。
- 使用 RefreshDatabase 或事务隔离。
- 测试使用 Factory。
- 外部接口使用 Http::fake。
- 队列使用 Queue::fake。
- 事件使用 Event::fake。
- 存储使用 Storage::fake。
- 时间相关使用 Carbon::setTestNow。
- 测试成功与失败路径。
- 测试权限边界。
- 测试重复提交。
- 测试并发风险时说明限制。
- 测试命名表达行为。
- 不依赖真实 Redis 和第三方。

## 22. 性能
- 优先减少数据库查询。
- 优先减少网络调用。
- 避免重复序列化。
- 列表接口必须分页。
- 导出必须限制范围。
- 大任务必须异步。
- 图片处理必须异步。
- 外部接口必须设置超时。
- 外部接口必须限制重试。
- 慢查询需要索引分析。
- 不为了微小收益增加复杂度。
- 优化前先确认瓶颈。

## 23. 提交检查
- 运行 php -l。
- 运行相关测试。
- 检查路由。
- 检查迁移。
- 检查权限。
- 检查 N+1。
- 检查事务。
- 检查缓存失效。
- 检查日志敏感信息。
- 检查队列重启要求。
- 检查配置缓存要求。
- 检查向后兼容。
