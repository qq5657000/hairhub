---
description: HairHub SEO 与公开内容页面规范
globs:
  - "resources/views/**/*.php"
  - "resources/js/**/*.{js,ts,vue}"
  - "app/Http/Controllers/**/*Article*.php"
  - "app/Http/Controllers/**/*Seo*.php"
  - "app/Models/Article.php"
  - "routes/web.php"
alwaysApply: false
---

# SEO 开发规范

## 1. 适用范围
- 仅用于公开可索引页面。
- 管理后台、登录页、用户中心不适用。
- API JSON 不添加页面 SEO 标签。
- SEO 规则不得影响核心业务正确性。
- 优先保证可访问性、速度和内容质量。
- 不使用隐藏文字、关键词堆砌等作弊方式。

## 2. URL
- URL 简短、稳定、可读。
- 使用小写和短横线。
- 文章优先使用唯一 slug。
- slug 不使用中文标点和随机参数。
- 已发布 URL 不随意修改。
- 修改 URL 时设置 301 重定向。
- 同一内容只保留一个规范 URL。
- 筛选、排序、分页参数避免产生重复索引。
- 路由不得暴露数据库结构。
- 页面 URL 不携带敏感信息。

## 3. Title 与 Description
- 每个可索引页面必须有唯一 title。
- title 先写核心主题，再写品牌。
- 避免标题重复和关键词堆砌。
- title 建议控制在约 30 个中文字符内。
- 每页提供唯一 meta description。
- description 应准确概括页面内容。
- description 建议控制在 80 至 120 个中文字符。
- 无内容页面不得生成虚假描述。
- title 和 description 必须支持后台配置或自动回退。
- 自动回退模板保持简短。

## 4. 页面标题结构
- 每页只保留一个主要 H1。
- H1 与页面主题一致。
- 内容层级按 H2、H3 顺序组织。
- 不用标题标签仅做视觉样式。
- 不跳跃使用标题层级。
- 列表卡片标题不应全部使用 H1。
- 页面标题不能由图片完全替代。

## 5. Canonical 与 Robots
- 每个公开页面输出 canonical。
- canonical 使用绝对 URL。
- canonical 指向首选版本。
- 搜索、筛选、预览页默认 noindex。
- 登录、注册、用户中心默认 noindex。
- 后台、回调、内部工具禁止索引。
- 已删除内容返回 404 或 410。
- 不使用 robots.txt 隐藏敏感数据。
- robots.txt 仅控制抓取，不承担权限控制。
- 分页页是否索引需保持统一策略。

## 6. Sitemap
- 生成 sitemap.xml。
- 仅包含可公开访问的规范 URL。
- 不包含 noindex 页面。
- 不包含登录后页面。
- 不包含失效或重定向 URL。
- 文章、发型、图库可拆分 sitemap。
- 大量 URL 使用 sitemap index。
- lastmod 只在内容实际变化时更新。
- 发布、更新、删除内容后同步 sitemap。
- sitemap 生成失败不得影响主站访问。

## 7. 内容页面
- 文章正文必须服务用户搜索意图。
- 禁止批量生成高度重复内容。
- AI 生成内容发布前应支持人工审核。
- 标题、摘要、正文语义保持一致。
- 重要页面提供明确作者、更新时间。
- 内容更新时保留原 URL。
- 相关文章基于真实主题关联。
- 页面避免只有图片没有文字说明。
- 空内容、测试内容不得公开索引。
- 草稿与预览页面必须 noindex。

## 8. 图片 SEO
- img 必须提供准确 alt。
- alt 描述图片内容，不堆关键词。
- 装饰图片使用空 alt。
- 文件名使用可读英文或拼音短语。
- 输出 width 和 height，减少布局抖动。
- 优先使用 WebP 或 AVIF，并保留兼容方案。
- 大图使用响应式尺寸。
- 首屏关键图片谨慎懒加载。
- 非首屏图片使用 lazy loading。
- 图片压缩不得明显影响发型细节。
- 用户隐私图片默认不得被搜索引擎索引。

## 9. 结构化数据
- 文章页可使用 Article 或 BlogPosting。
- 面包屑可使用 BreadcrumbList。
- FAQ 仅在页面真实展示问答时使用。
- 商品页仅在数据真实完整时使用 Product。
- 结构化数据必须与页面可见内容一致。
- 禁止伪造评分、价格、库存。
- JSON-LD 字段必须转义并校验。
- 缺失数据时省略字段，不填虚假值。
- 一个页面避免重复冲突的结构化数据。

## 10. Open Graph 与分享
- 公开页面提供 og:title。
- 提供 og:description。
- 提供 og:url。
- 提供 og:image。
- og:url 与 canonical 一致。
- 分享图使用绝对 HTTPS 地址。
- 分享图尺寸和文件大小适合微信及社交平台。
- 文章更新后同步分享信息。
- 微信分享配置不得影响普通 SEO 标签。

## 11. 性能
- 首屏避免加载无关大型脚本。
- CSS、JS、图片按需加载。
- 避免重复请求相同资源。
- 静态资源使用版本号和缓存。
- 页面不得因 SEO 数据额外产生 N+1 查询。
- 服务端渲染页面避免阻塞第三方请求。
- 第三方统计脚本异步加载。
- 图片生成缩略图，禁止列表加载原图。
- 列表分页，禁止一次加载全部内容。
- 缓存不能导致已删除内容长期可访问。

## 12. 移动端与可访问性
- 页面必须适配移动端。
- 字体大小应可正常阅读。
- 点击区域不能过小。
- 重要内容不依赖 hover。
- 表单字段提供 label。
- 链接文本应表达目标。
- 不使用“点击这里”作为唯一链接文案。
- 图片、按钮、导航支持基本无障碍。
- 弹窗不得阻挡搜索引擎和用户读取主要内容。

## 13. 重复内容
- 同一文章不得存在多个可索引 URL。
- http 统一重定向到 https。
- www 与非 www 只保留一个版本。
- 尾斜杠策略保持统一。
- 大小写 URL 统一。
- UTM 等追踪参数不作为 canonical。
- 打印页、预览页、分享中间页设置 noindex 或 canonical。
- 标签页和分类页必须有独立价值才允许索引。
- 低质量聚合页默认 noindex。

## 14. 状态码
- 正常页面返回 200。
- 永久迁移返回 301。
- 临时迁移返回 302 或 307。
- 不存在返回 404。
- 永久删除可返回 410。
- 服务异常返回 5xx。
- 禁止不存在页面返回 200。
- 禁止所有错误重定向到首页。
- 软 404 页面必须修正。

## 15. 发布检查
- 检查 title 是否唯一。
- 检查 description 是否准确。
- 检查 H1 是否唯一。
- 检查 canonical。
- 检查 robots。
- 检查 sitemap。
- 检查状态码。
- 检查图片 alt 与尺寸。
- 检查结构化数据。
- 检查移动端显示。
- 检查页面速度。
- 检查重复 URL。
- 检查草稿和隐私内容未被索引。
