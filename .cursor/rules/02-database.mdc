---
description: MySQL 数据库规范
globs:
  - "database/**/*.php"
  - "app/Models/**/*.php"
  - "**/*.sql"
alwaysApply: false
---

# MySQL 数据库规范

## 1. 基础原则
- 数据库使用 MySQL 8。
- 默认字符集 utf8mb4。
- 默认排序规则 utf8mb4_unicode_ci。
- 表名使用 snake_case 复数。
- 字段名使用 snake_case。
- 主键默认使用 id。
- 主键优先 bigint unsigned。
- 外键字段使用 {model}_id。
- 时间字段使用 created_at、updated_at。
- 软删除使用 deleted_at。
- 不使用中文表名或字段名。
- 不使用保留字。
- 不使用无意义缩写。
- 表和字段必须有注释。
- 设计优先满足数据一致性。
- 避免过度范式化。
- 避免无边界 JSON 化。

## 2. 字段类型
- ID 使用 unsignedBigInteger。
- 小范围枚举可用 unsignedTinyInteger。
- 状态字段优先 tinyint。
- 布尔字段使用 boolean 或 tinyint(1)。
- 金额使用 decimal 或整数分。
- 禁止 float 保存金额。
- 比例使用 decimal。
- 普通字符串使用 varchar。
- 长文本使用 text。
- 超长文本才使用 longText。
- 时间点使用 timestamp 或 datetime。
- 日期使用 date。
- JSON 数据使用 json。
- IP 地址使用 varchar(45)。
- 手机号使用 varchar。
- 邮编使用 varchar。
- 第三方编号使用 varchar。
- UUID 使用 char(36) 或 binary(16)。
- 不确定长度时不要盲目 varchar(255)。
- 字段长度按业务范围设置。

## 3. 空值与默认值
- 字段是否 nullable 必须有业务含义。
- 必填字段设置 not null。
- 状态字段提供明确默认值。
- 计数字段默认 0。
- 布尔字段默认 false。
- JSON 字段默认值需兼容 MySQL。
- 时间字段不使用 0000-00-00。
- 不用空字符串代替 null。
- null 与 0 的语义必须区分。
- null 与空数组的语义必须区分。
- 新增非空字段需考虑历史数据。
- 迁移必须提供安全默认值或分步执行。

## 4. 索引
- 主键自动建立索引。
- 高频查询条件建立索引。
- 高频排序字段建立索引。
- 关联字段建立索引。
- 唯一业务标识建立唯一索引。
- 联合索引按过滤选择性排序。
- 联合索引遵循最左前缀。
- 索引字段数量尽量少。
- 避免重复索引。
- 避免冗余前缀索引。
- 低选择性字段不单独建索引。
- 大文本不直接建普通索引。
- JSON 查询考虑生成列索引。
- 索引名表达字段。
- 普通索引：idx_table_field。
- 唯一索引：uk_table_field。
- 外键索引：idx_table_foreign_id。
- 新增索引前检查现有索引。
- 删除索引前确认线上查询。
- 大表建索引需评估锁表风险。

## 5. 唯一性
- 业务唯一性优先数据库保证。
- 唯一校验不能只依赖代码。
- 软删除表的唯一性需明确策略。
- 可使用 deleted_at 参与唯一约束。
- 可使用 generated column 支持唯一约束。
- 第三方回调单号必须唯一。
- 幂等键必须唯一。
- 用户与资源关系需联合唯一。
- 唯一冲突需转换为业务错误。
- 并发写入不能使用“先查再插”保证唯一。

## 6. 外键
- 是否使用物理外键按项目现状统一。
- 不混用无规则的外键策略。
- 使用外键时明确删除行为。
- 禁止默认级联删除核心数据。
- 核心业务优先 restrict。
- 从属数据可使用 cascade。
- 可空关联可使用 set null。
- 不使用外键时由服务层保证一致性。
- 外键字段类型必须一致。
- 外键字段必须有索引。
- 跨库关系不使用物理外键。
- 高频写大表使用外键前评估性能。

## 7. 状态字段
- 状态值必须定义枚举。
- 0 不应默认代表所有未知含义。
- 状态名称保持稳定。
- 状态变更必须校验前置状态。
- 状态流转记录日志。
- 复杂流程使用状态历史表。
- 禁止直接覆盖关键历史状态。
- 状态字段建立索引前评估选择性。
- 删除状态与软删除不要混淆。
- 启用状态命名 is_enabled 或 status。

## 8. JSON 字段
- JSON 只用于结构变化频繁的数据。
- 核心查询字段不得只放 JSON。
- 核心关联 ID 不放 JSON。
- 核心状态不放 JSON。
- JSON 结构必须有文档。
- JSON 键使用 snake_case。
- JSON 更新需防止覆盖并发修改。
- 高频 JSON 条件考虑生成列。
- 大 JSON 不频繁全量返回。
- 不把整个对象序列化进 JSON 代替建模。
- JSON 字段需定义 Laravel cast。
- 历史快照可使用 JSON。

## 9. 迁移
- 每次结构变化新建迁移。
- 禁止修改已上线迁移。
- 迁移文件名表达动作。
- up() 与 down() 尽量对称。
- down() 不得误删无关数据。
- 数据迁移与结构迁移尽量分开。
- 大表迁移避免长事务。
- 大表新增非空字段采用分步方案。
- 大表修改字段类型需评估锁。
- 删除字段前先停止代码引用。
- 字段重命名需兼容旧代码。
- 索引变更需评估执行时间。
- 迁移中避免调用业务 Model。
- 迁移使用 Schema 或 DB。
- 迁移不依赖当前业务代码。
- 发布前备份关键数据。
- 高风险迁移提供回滚方案。
- 迁移后验证数据完整性。

## 10. 数据迁移
- 大数据迁移使用命令或 Job。
- 不在普通 migration 全量遍历模型。
- 使用 chunkById。
- 支持断点续跑。
- 支持 dry-run。
- 记录处理进度。
- 记录失败数据。
- 单批次事务。
- 控制批次大小。
- 避免长时间锁表。
- 重跑必须幂等。
- 数据修复前先备份。
- 数据修复后输出统计。

## 11. 查询规范
- 禁止 SELECT *。
- 只查询必要字段。
- 列表必须分页。
- 深分页优先游标分页。
- 避免函数包裹索引字段。
- 避免前缀通配 LIKE。
- 避免隐式类型转换。
- 避免 OR 导致索引失效。
- 复杂 OR 可拆分 UNION。
- EXISTS 优先用于存在判断。
- 批量查询代替循环查询。
- 使用 JOIN 时字段类型一致。
- 多表查询明确主表。
- 排序必须稳定。
- 分页排序包含唯一字段。
- 聚合查询限制时间范围。
- 报表查询与业务查询隔离。
- 慢查询使用 EXPLAIN 分析。
- 不凭感觉新增索引。

## 12. 写入规范
- 批量写入使用 insert 或 upsert。
- 避免循环单条 insert。
- 更新必须有明确 where。
- 删除必须有明确 where。
- 禁止无条件 update。
- 禁止无条件 delete。
- 关键更新检查受影响行数。
- 并发扣减使用原子更新。
- 余额与库存不能读后写。
- 使用 increment、decrement 或条件更新。
- 写入失败必须回滚。
- 重复回调使用唯一键防重。
- 高并发写入避免热点行。
- 写操作需考虑主从延迟。

## 13. 事务与锁
- 事务范围尽量小。
- 事务内不调用第三方接口。
- 事务内不执行文件处理。
- 需要一致性时使用行锁。
- 锁定顺序保持一致。
- 避免大范围锁定。
- 使用 lockForUpdate 前确认索引。
- 无索引条件可能锁大量行。
- 死锁需有限重试。
- 重试操作必须幂等。
- 隔离级别按默认使用。
- 特殊隔离级别必须说明原因。
- 长事务必须拆分。
- 事务日志不可无限增长。

## 14. 软删除
- 核心业务数据优先软删除。
- 临时表可物理删除。
- 查询需明确是否包含已删除。
- 唯一约束需兼容软删除。
- 恢复操作需检查冲突。
- 关联查询需明确删除数据行为。
- 后台展示删除状态需区分。
- 定期清理需保留审计要求。
- 物理清理使用批次任务。
- 删除前确认文件与关联数据。

## 15. 审计与历史
- 关键操作记录操作人。
- 关键表可增加 created_by、updated_by。
- 重要状态变更记录历史表。
- 支付与订单保留完整流水。
- AI 生成任务保留请求与结果引用。
- 敏感数据访问可记录审计日志。
- 历史表避免被业务更新。
- 日志表按时间归档。
- 日志表避免过多二级索引。
- 审计数据不可记录密钥明文。

## 16. 性能与容量
- 设计时估算数据量。
- 大表提前规划归档。
- 高频日志按月或按日归档。
- 图片文件不存数据库。
- 大文本与主表可拆分。
- 冷热数据可拆分。
- 不因预估过度分库分表。
- 达到明确瓶颈再分片。
- 读写分离需考虑延迟。
- 统计数据可异步汇总。
- 热点统计可使用 Redis。
- 数据库仍是最终事实来源。

## 17. 命名示例
- 用户表：users。
- 发型表：hairstyles。
- 生成任务表：hair_generation_tasks。
- 发型历史表：hair_generation_histories。
- 商品表：products。
- 文章表：articles。
- 标签关系表：article_tag。
- 用户收藏表：user_favorites。
- 状态字段：status。
- 启用字段：is_enabled。
- 排序字段：sort_order。
- 外部单号：external_order_no。
- 幂等键：idempotency_key。

## 18. 变更检查
- 检查字段类型。
- 检查默认值。
- 检查 null 语义。
- 检查索引。
- 检查唯一约束。
- 检查历史数据。
- 检查迁移回滚。
- 检查大表锁风险。
- 检查代码兼容。
- 检查主从延迟。
- 检查备份方案。
- 检查上线顺序。

## 19. 数据库破坏性操作安全规则

- 严禁对开发库、生产库执行 migrate:fresh、migrate:refresh、db:wipe。
- 严禁执行 DROP DATABASE、DROP TABLE、TRUNCATE，除非用户明确批准。
- 所有自动测试只能使用数据库名包含 `_test` 的独立测试库。
- 运行测试前必须输出并确认：
  - APP_ENV=testing
  - 当前数据库名称
- 当前数据库名称不包含 `test` 时，必须立即停止测试。
- 禁止由临时脚本动态删除或重建数据库。
- 创建测试数据库前必须核对目标数据库名，禁止使用 env('DB_DATABASE') 作为待删除数据库。
- 临时验证优先使用事务回滚或 PHPUnit，不得操作开发库真实数据。
- 任何破坏性数据库命令执行前，必须先向用户说明影响并等待确认。
