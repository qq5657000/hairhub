---
description: HairHub 安全开发规范
globs:
  - "app/**/*.php"
  - "routes/**/*.php"
  - "config/**/*.php"
  - "database/**/*.php"
alwaysApply: false
---

# 安全开发规范

## 1. 基本原则
- 所有外部输入均不可信。
- 所有权限必须由服务端校验。
- 默认拒绝，按需放行。
- 修改范围最小，不降低现有安全等级。
- 禁止为了调试绕过认证、鉴权、验签或限流。
- 安全配置必须来自环境变量。
- 密钥、Token、密码不得写入代码或日志。
- 生产环境必须关闭 debug。

## 2. 认证与会话
- 当前用户必须从认证上下文获取。
- 禁止信任前端传入的 user_id。
- Token 必须设置有效期。
- Token 不得出现在 URL。
- 登出、封禁、改密后应使相关 Token 失效。
- 管理端与用户端认证体系应隔离。
- 登录接口必须限流。
- 连续失败应增加等待时间或触发风控。
- 敏感操作可要求二次验证。
- 不返回“账号存在与否”等可枚举信息。

## 3. 权限与越权
- 每个受保护接口必须校验权限。
- 资源操作必须校验所属用户或租户。
- 前端按钮隐藏不能代替后端鉴权。
- 批量操作必须逐项或按查询范围校验权限。
- 管理员权限使用 Policy、Gate 或统一权限服务。
- 禁止通过修改 ID 访问其他用户数据。
- 导出、下载、预览同样需要权限校验。
- 无权限返回 403。
- 不存在与无权限可按安全策略统一处理。

## 4. 输入验证
- 写接口优先使用 FormRequest。
- 字符串必须限制长度。
- 数组必须限制数量并校验每一项。
- 数字必须限制范围。
- 枚举必须使用白名单。
- 排序字段和筛选字段必须使用白名单。
- 禁止客户端传入 SQL、列名或表达式直接执行。
- 富文本必须经过可信 HTML 清洗。
- URL 必须校验协议与域名。
- 回调地址不得允许任意内网地址。
- 防止 SSRF 访问 localhost、内网 IP 和云元数据地址。

## 5. SQL 与数据写入
- 禁止拼接 SQL。
- 原生 SQL 必须使用参数绑定。
- 禁止无条件 update、delete。
- 批量赋值必须配置 fillable 或明确字段。
- 价格、额度、折扣、权限状态不得信任客户端。
- 金额使用整数分或 Decimal。
- 订单、额度、任务扣费必须使用事务。
- 重复请求必须使用唯一约束或幂等键。
- Redis 锁不能替代数据库唯一约束。
- 高并发更新使用原子更新或行锁。

## 6. 文件上传
- 校验文件大小、MIME、扩展名。
- 不仅依赖客户端 MIME。
- 文件名必须重新生成。
- 禁止使用用户文件名作为存储路径。
- 上传目录不得允许脚本执行。
- 图片处理前校验是否为有效图片。
- 限制图片像素尺寸，防止解压炸弹。
- 私有文件使用授权下载或临时签名 URL。
- 禁止返回服务器绝对路径。
- 删除业务数据时处理文件生命周期。
- 高风险文件类型默认拒绝。

## 7. 第三方接口与回调
- 所有请求必须设置连接与读取超时。
- 重试次数必须有限。
- POST 重试必须保证幂等。
- 回调必须校验签名、时间戳和随机数。
- 签名比较使用常量时间比较。
- 回调业务单号必须建立唯一索引。
- 防止重复回调造成重复扣费或重复发放。
- 第三方错误不得原样返回给客户端。
- 日志仅记录脱敏摘要。
- 外部 URL 必须防 SSRF。
- 第三方密钥通过 config() 读取。

## 8. AI 生成业务
- AI 任务必须校验用户身份和额度。
- 任务结果只能由任务所属用户访问。
- 重试任务不得重复扣减额度。
- 生成任务和扣费操作必须幂等。
- 用户上传图片必须设置访问权限和生命周期。
- 不在日志中记录完整人脸图片地址。
- 第三方 AI 回调必须验签和防重。
- 失败任务必须明确是否退回额度。
- 管理员查看用户图片必须有审计记录。
- 涉及人脸、深度合成时保留必要合规标识和授权记录。

## 9. XSS、CSRF 与 CORS
- Blade 输出默认使用转义语法。
- 禁止直接输出未经清洗的 HTML。
- 富文本仅允许白名单标签和属性。
- Web 表单必须启用 CSRF。
- API 使用 Token 时按架构配置 CSRF。
- CORS 只允许明确域名。
- 携带凭证时禁止 Access-Control-Allow-Origin: *。
- 禁止反射任意 Origin。
- Cookie 设置 HttpOnly、Secure、SameSite。
- 敏感页面设置合理的安全响应头。

## 10. 日志与隐私
- 禁止记录密码、验证码、Token、Secret。
- 手机号、邮箱、身份证等必须脱敏。
- 请求体日志必须过滤敏感字段。
- 异常日志保留 request_id、user_id、业务 ID。
- 生产响应不返回异常堆栈。
- 审计日志记录操作人、对象、时间和结果。
- 日志访问权限遵循最小权限。
- 日志保存周期按业务与合规要求设置。

## 11. 配置与依赖
- .env 不得提交仓库。
- 示例配置不得包含真实密钥。
- 禁止修改 vendor。
- 新依赖必须说明用途和维护状态。
- 避免引入无人维护的安全相关包。
- Composer 依赖应定期审计。
- 生产环境禁用无用调试工具。
- 配置缓存后不得在业务代码直接 env()。
- 默认账号和默认密码必须移除。

## 12. 输出检查
- 检查认证与权限。
- 检查资源归属。
- 检查参数白名单。
- 检查 SQL 注入风险。
- 检查批量赋值。
- 检查文件上传。
- 检查 SSRF。
- 检查 XSS 与 CSRF。
- 检查幂等与并发。
- 检查日志敏感信息。
- 检查错误信息泄露。
- 检查测试是否覆盖越权和重复请求。
