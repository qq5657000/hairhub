---
description: HairHub V1.0 项目总规则，所有开发任务必须遵循
globs:
alwaysApply: true
---

# HairHub V1.0 项目总规则

本规则是 HairHub 项目的最高级开发规则。

Cursor 在任何开发、代码修改、Bug 分析、架构设计任务开始前，必须遵守本规则。

---

# 一、交流语言规则

默认使用中文与用户交流。

所有以下内容必须使用中文：

- 需求分析
- 技术方案
- 修改计划
- Bug 分析
- 风险说明
- 测试说明
- 开发总结

代码相关内容保持英文：

- 类名
- 方法名
- 变量名
- 数据库表名
- 字段名
- API路径
- Git Commit Message

除非用户明确要求，否则不要使用英文解释开发过程。

---

# 二、项目定位

HairHub 是一个 AI 发型内容平台。

产品定位：

> 一个以 AI 工具为入口，以发型内容为核心，以 SEO 为增长引擎，以微信生态和短视频矩阵为转化闭环的平台。

核心业务：

- AI 换发型
- AI 换发色
- 发型分类
- 发型图库
- 发型详情
- 发色中心
- 发色推荐
- 内容中心
- 视频中心
- SEO 内容增长
- 微信生态转化

---

# 三、产品使命

HairHub 的产品使命：

> 帮助每个人在剪发、染发之前，先看到最适合自己的样子。

---

# 四、V1.0 开发目标

V1.0 的目标：

快速上线。

优先：

- AI工具可使用
- 内容系统可运营
- SEO页面可收录
- 图片资源可积累
- 视频资源可沉淀
- 微信生态可转化

不要追求一次完成所有功能。

---

# 五、技术架构原则

当前技术：

- Laravel 10
- PHP 8.2+
- MySQL 8+
- Dcat Admin / Dcat Plus
- Laravel 原生能力
- 独立 AI API 服务

核心原则：

## 5.1 Laravel 原生优先

优先使用：

- Controller
- Model
- Service
- Migration
- Queue
- Scheduler
- Storage
- Cache

不要为了架构漂亮而增加复杂设计。

---

## 5.2 简单优先

V1.0 不主动引入：

- DDD
- CQRS
- Event Sourcing
- 微服务
- 复杂 Repository
- 过度抽象架构

除非用户明确要求。

---

## 5.3 保持 Laravel 传统结构

遵循：

```text
app/
├── Admin/
├── Http/
├── Models/
├── Services/
├── Jobs/
└── Providers/
```

不要擅自创建：

```text
app/Domain
app/Application
app/Infrastructure
app/Modules
```

---

# 六、AI 架构原则

HairHub 不负责 AI 模型运行。

当前 AI：

- HairFastGAN
- 独立 4090D GPU服务器
- HTTP API调用

架构：

```text
用户
 ↓
HairHub API
 ↓
AiService
 ↓
AI Provider
 ↓
HairFastGAN API
 ↓
返回结果
```

禁止：

- Controller 直接调用 AI API
- Model 中调用 AI API
- 把 AI 模型代码放入 HairHub

---

# 七、开发前必须阅读文档

开发任何功能前，必须先阅读相关设计文档。

项目设计文档：

```text
docs/v1.0/

01-项目定位.md
02-系统架构.md
03-模块设计.md
04-数据库设计.md
05-API设计规范.md
06-Dcat后台设计.md
07-前端页面设计.md
08-开发任务拆解.md
09-SEO设计.md
10-部署架构.md
11-开发规范.md
12-AI设计.md
13-微信生态设计.md
```

---

# 八、开发流程要求

任何代码修改必须遵循：

```text
阅读相关文档

↓

检查现有代码

↓

寻找已有类似实现

↓

分析方案

↓

修改代码

↓

测试验证

↓

输出修改总结
```

---

# 九、修改代码原则

## 9.1 小范围修改

只修改当前任务相关代码。

禁止：

- 修改无关模块
- 大面积重构
- 重命名大量文件
- 移动项目结构
- 替换已有技术方案

---

## 9.2 不猜测

如果发现：

- 数据库字段不存在
- API规则不明确
- 文档和代码冲突
- 第三方接口不确定

不要直接猜测。

必须：

1. 说明发现的问题
2. 给出解决方案
3. 等待确认

---

## 9.3 不擅自新增依赖

禁止直接新增：

- Composer 包
- NPM 包
- 第三方服务

如果需要新增：

必须说明：

- 为什么需要
- 解决什么问题
- 是否有替代方案
- 影响范围

---

# 十、数据库原则

数据库设计以：

```text
docs/v1.0/04-数据库设计.md
```

为准。

要求：

- 使用 Migration
- 不手工修改生产数据库
- 不猜测字段
- 不随意修改已有结构

---

# 十一、API原则

API设计以：

```text
docs/v1.0/05-API设计规范.md
```

为准。

要求：

- 统一版本
- 统一返回格式
- 不随意修改接口结构
- 保持前后端兼容

---

# 十二、后台原则

后台设计以：

```text
docs/v1.0/06-Dcat后台设计.md
```

为准。

后台使用：

Dcat Admin

禁止重新开发后台系统。

---

# 十三、媒体文件原则

所有图片必须统一管理。

原则：

```text
上传

↓

MediaService

↓

保存

↓

生成缩略图

↓

记录media_files
```

禁止：

- 各模块自行上传
- 随意保存图片路径
- 不记录媒体信息

---

# 十四、SEO原则

SEO设计以：

```text
docs/v1.0/09-SEO设计.md
```

为准。

公开内容页面需要考虑：

- Title
- Description
- Slug
- Canonical
- Sitemap
- 图片Alt
- 内链

---

# 十五、微信生态原则

微信生态设计以：

```text
docs/v1.0/13-微信生态设计.md
```

为准。

流量路径：

网站：

```text
SEO流量
 ↓
HairHub内容
 ↓
AI工具
 ↓
微信生态
```

短视频：

```text
视频号
 ↓
服务号 / 小程序 / 微信小店


抖音
快手
小红书
B站

 ↓

微信小程序

 ↓

微信小店
```

---

# 十六、Cursor 输出要求

每次完成开发后，必须输出：

```text
【完成内容】

【修改文件】

【新增文件】

【数据库变化】

【API变化】

【测试方法】

【风险说明】

【后续建议】

【Git Commit建议】
```

---

# 十七、禁止事项

禁止：

- 擅自改变架构
- 擅自修改数据库设计
- 擅自修改API规范
- Controller 写复杂业务
- Model 调用外部API
- 硬编码密钥
- 提交.env
- 提交用户隐私数据
- 吞掉异常
- 忽略错误日志
- 大规模重构

---

# 十八、项目长期原则

HairHub 开发原则：

> 先上线，再积累，再优化。

优先保证：

- 能运行
- 能运营
- 能获取流量
- 能沉淀内容
- 能持续扩展

不要为了未来未知需求提前增加复杂度。

最终目标：

> 使用简单、稳定、可维护的系统，让 HairHub 持续积累发型数据、内容资产、SEO流量和微信生态价值。
