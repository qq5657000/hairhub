---
description: REST API 设计规范
globs:
  - "routes/api.php"
  - "app/Http/Controllers/Api/**/*.php"
  - "app/Http/Requests/**/*.php"
  - "app/Http/Resources/**/*.php"
alwaysApply: false
---

# REST API 规范

## 1. 基础约定
- API 使用 HTTPS。
- 数据格式统一 JSON。
- 字符编码 UTF-8。
- 接口前缀使用 /api/v1。
- URL 使用小写。
- URL 使用 kebab-case。
- 资源名使用复数。
- 不在 URL 使用动词。
- 特殊动作可使用子资源。
- 时间使用 ISO 8601。
- 默认时区明确。
- 金额明确单位。
- ID 类型保持稳定。
- 布尔值使用 true/false。
- null 与空字符串语义区分。
- 不返回 HTML 错误页。
- 不暴露内部异常。

## 2. URL 设计
- 列表：GET /api/v1/articles。
- 详情：GET /api/v1/articles/{article}。
- 新增：POST /api/v1/articles。
- 修改：PUT /api/v1/articles/{article}。
- 局部修改：PATCH /api/v1/articles/{article}。
- 删除：DELETE /api/v1/articles/{article}。
- 子资源：GET /users/{user}/favorites。
- 创建任务：POST /hair-generation-tasks。
- 查询任务：GET /hair-generation-tasks/{task}。
- 取消任务：POST /hair-generation-tasks/{task}/cancel。
- 非 CRUD 动作必须语义明确。
- URL 不包含 get、create、update 等冗余动词。
- URL 不暴露数据库表名。
- 嵌套层级不超过两层。
- 复杂筛选使用查询参数。

## 3. HTTP 方法
- GET：读取，不产生副作用。
- POST：创建资源或执行动作。
- PUT：完整替换。
- PATCH：局部更新。
- DELETE：删除资源。
- GET 请求不得修改数据。
- DELETE 必须支持权限校验。
- 重复 DELETE 可返回 204 或业务定义结果。
- POST 创建成功返回 201。
- 异步任务创建可返回 202。
- 成功无内容返回 204。
- 不用 POST 代替所有操作。

## 4. 请求头
- Content-Type: application/json。
- Accept: application/json。
- 认证使用 Authorization。
- Bearer Token 放请求头。
- 幂等键使用 Idempotency-Key。
- 请求追踪使用 X-Request-Id。
- 语言可使用 Accept-Language。
- 客户端版本可使用 X-App-Version。
- 不在 URL 传敏感 Token。
- 服务端返回 X-Request-Id。

## 5. 认证
- 客户端 API 使用项目统一认证。
- 后台 API 使用独立认证与权限。
- Token 必须有过期机制。
- Token 不写入日志。
- 刷新 Token 与访问 Token 分离。
- 登出需使 Token 失效。
- 敏感操作可要求二次验证。
- 不信任前端传入 user_id。
- 当前用户从认证上下文获取。
- 匿名接口明确标记。
- 未认证返回 401。
- 无权限返回 403。
- 用户资源必须校验归属。

## 6. 权限
- 权限校验必须在服务端。
- 使用 Policy 或 Gate。
- 管理端按钮权限不能替代后端权限。
- 资源级权限校验归属。
- 角色权限与数据权限分开。
- 超级管理员权限显式处理。
- 跨租户访问必须禁止。
- 权限失败不泄露资源详情。
- 不存在与无权限可按安全策略统一返回。
- 权限变更后缓存立即失效。

## 7. 参数规范
- 路径参数用于资源标识。
- 查询参数用于筛选、排序、分页。
- 请求体用于创建或修改。
- 字段使用 snake_case。
- 参数名保持稳定。
- 不使用模糊缩写。
- 枚举值使用稳定英文值或整数。
- 日期参数明确格式。
- 时间范围使用 start_time、end_time。
- 布尔筛选使用 is_enabled。
- 数组参数必须定义最大长度。
- 字符串必须定义最大长度。
- 文件上传使用 multipart/form-data。
- JSON 请求不混用表单参数。
- 敏感参数不得出现在 URL。
- 未知字段可拒绝或忽略，但策略统一。

## 8. 参数验证
- 所有输入必须验证。
- 写接口使用 FormRequest。
- 必填项使用 required。
- 可空项使用 nullable。
- ID 使用 exists。
- 枚举使用 Rule::enum。
- 唯一字段使用 unique。
- 更新唯一校验忽略当前资源。
- 数字设置范围。
- 字符串设置长度。
- 数组校验每一项。
- 文件校验类型与大小。
- 图片校验尺寸。
- 日期校验格式与顺序。
- 关联资源校验归属。
- 422 返回字段级错误。
- 验证失败不进入业务逻辑。

## 9. 统一响应
- 响应结构保持统一。
- 成功响应包含 code、message、data。
- 失败响应包含 code、message、errors。
- code 为稳定业务码。
- message 面向用户或调用方。
- data 为对象、数组或 null。
- errors 用于字段级详情。
- meta 用于分页等元数据。
- request_id 用于追踪。
- 不使用 HTTP 200 包装所有错误。
- 不返回数据库字段原始异常。
- 不返回 stack trace。
- 不返回 SQL。
- 不返回文件系统路径。

## 10. HTTP 状态码
- 200：成功。
- 201：创建成功。
- 202：已接受异步处理。
- 204：成功无内容。
- 400：请求格式错误。
- 401：未认证。
- 403：无权限。
- 404：资源不存在。
- 409：资源冲突。
- 422：参数验证失败。
- 429：请求过多。
- 500：服务器错误。
- 502：上游服务错误。
- 503：服务不可用。
- 504：上游超时。
- 不滥用 500 表达业务失败。

## 11. 分页
- 列表接口默认分页。
- 参数使用 page、per_page。
- per_page 设置最大值。
- 默认值统一。
- meta 包含 current_page。
- meta 包含 per_page。
- meta 包含 total。
- meta 包含 last_page。
- 深分页优先 cursor。
- 游标参数使用 cursor。
- 排序必须稳定。
- 排序包含唯一字段。
- 禁止无上限列表接口。
- 导出接口与列表接口分离。

## 12. 筛选与排序
- 精确筛选使用 field=value。
- 多值筛选可使用逗号或数组，项目内统一。
- 时间范围使用 start_time、end_time。
- 关键词使用 keyword。
- 排序参数使用 sort。
- 降序可用 -created_at。
- 只允许白名单排序字段。
- 只允许白名单筛选字段。
- 禁止客户端传原始 SQL。
- 禁止客户端传任意列名。
- 搜索关键词限制长度。
- 模糊搜索防止全表扫描。

## 13. 字段输出
- 使用 Resource 控制字段。
- 不暴露密码。
- 不暴露 Token。
- 不暴露密钥。
- 不暴露内部路径。
- 不暴露软删除字段。
- 不暴露内部备注。
- 用户隐私字段按权限返回。
- 时间字段格式统一。
- 金额字段同时明确单位。
- 枚举可返回 value 与 label。
- 关联数据按需返回。
- 避免返回巨大嵌套对象。
- 可用 include 控制关联，但必须白名单。
- 字段删除属于破坏性变更。
- 新增字段应保持向后兼容。

## 14. 文件上传
- 上传接口单独设计。
- 限制文件类型。
- 限制文件大小。
- 图片限制宽高。
- 服务端重新生成文件名。
- 返回 file_id。
- 业务接口优先引用 file_id。
- 私有文件不返回永久公开地址。
- 使用临时签名 URL。
- 上传后病毒或内容检测按需执行。
- 图片处理使用异步任务。
- 删除资源时处理文件生命周期。
- 不信任扩展名。
- 不返回服务器绝对路径。

## 15. AI 生成任务
- AI 生成使用异步任务模型。
- 创建任务返回 202。
- 返回 task_id。
- 状态包括 pending、processing、succeeded、failed、cancelled。
- 状态值保持稳定。
- 进度为可选字段。
- 失败返回稳定错误码。
- 不直接返回第三方原始错误。
- 任务结果使用独立字段或资源。
- 任务查询必须校验用户归属。
- 创建任务必须校验套餐与额度。
- 扣减额度必须幂等。
- 重试任务必须避免重复扣费。
- 回调必须校验签名。
- 回调必须使用唯一单号防重。
- 超时任务需有补偿机制。
- 任务结果文件设置生命周期。
- 日志记录 task_id 与 request_id。

## 16. 幂等
- 支付、扣费、任务创建需幂等。
- 客户端传 Idempotency-Key。
- 幂等键绑定用户与接口。
- 服务端限制键长度。
- 相同键相同请求返回相同结果。
- 相同键不同请求返回 409。
- 幂等记录设置合理 TTL。
- 数据库使用唯一索引最终防重。
- 不仅依赖 Redis。
- 第三方回调按外部单号幂等。
- 失败是否允许重试需明确。

## 17. 限流
- 登录接口限流。
- 验证码接口限流。
- 上传接口限流。
- AI 生成接口限流。
- 搜索接口限流。
- 匿名与登录用户规则分开。
- 套餐额度与技术限流分开。
- 超限返回 429。
- 返回 retry_after。
- 限流 Key 不包含敏感明文。
- 管理端限流独立设置。
- 白名单必须配置化。

## 18. 安全
- 全部输入验证。
- 全部输出脱敏。
- 防止越权访问。
- 防止批量赋值漏洞。
- 防止 SQL 注入。
- 防止文件上传攻击。
- 防止重放攻击。
- 回调验证签名与时间戳。
- 签名比较使用安全方法。
- 密钥不写代码。
- 日志过滤密码与 Token。
- URL 不传敏感信息。
- CORS 使用白名单。
- 不允许任意 Origin 携带凭证。
- 生产环境关闭 debug。
- 高风险操作记录审计日志。

## 19. 第三方接口
- 统一封装 Client。
- 设置连接超时。
- 设置请求超时。
- 设置有限重试。
- 重试仅用于可重试错误。
- POST 重试必须保证幂等。
- 记录请求耗时。
- 记录上游状态码。
- 日志脱敏。
- 上游错误转换为内部错误码。
- 不直接透传第三方文案。
- 熔断与降级按重要性设计。
- 回调必须验签。
- 回调失败返回明确状态。
- 必要时保存原始回调摘要。

## 20. 禁止事项
- 禁止所有错误返回 200。
- 禁止 Controller 直接返回异常。
- 禁止响应结构随接口变化。
- 禁止暴露内部模型全部字段。
- 禁止接口无限返回数据。
- 禁止 GET 修改数据。
- 禁止 URL 包含 Token。
- 禁止未校验资源归属。
- 禁止不设超时调用第三方。
- 禁止回调不验签。
- 禁止仅靠前端防重复。
- 禁止文档与实现长期不一致。
- 禁止未经版本升级进行破坏性修改。

## 21. 提交检查
- 检查路由命名。
- 检查认证。
- 检查权限。
- 检查验证规则。
- 检查响应结构。
- 检查状态码。
- 检查错误码。
- 检查分页。
- 检查字段脱敏。
- 检查幂等。
- 检查限流。
- 检查日志。
- 检查第三方超时。
- 检查测试。
- 检查文档。
- 检查向后兼容。
